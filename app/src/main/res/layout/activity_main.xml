<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnCAQP"
        android:layout_width="350dp"
        android:layout_height="90dp"
        android:layout_marginBottom="30dp"
        android:backgroundTint="#009688"
        android:text="Catedral Arequipa"
        android:textAlignment="center"
        android:textSize="20sp"
        app:layout_constraintBottom_toTopOf="@+id/btnCC"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnCC"
        android:layout_width="350dp"
        android:layout_height="90dp"
        android:layout_marginBottom="52dp"
        android:backgroundTint="#009688"
        android:text="Claustro de la compañia"
        android:textAlignment="viewEnd"
        android:textAllCaps="false"
        android:textIsSelectable="false"
        android:textSize="20sp"
        android:textStyle="normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.497"
        app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imgCatedral"
        android:layout_width="82dp"
        android:layout_height="82dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="@+id/btnCAQP"
        app:layout_constraintStart_toStartOf="@+id/btnCAQP"
        app:layout_constraintTop_toTopOf="@+id/btnCAQP"
        app:shapeAppearanceOverlay="@style/roundedImageViewRounded"
        app:srcCompat="@drawable/catedral" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imgCC"
        android:layout_width="82dp"
        android:layout_height="82dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="@+id/btnCC"
        app:layout_constraintStart_toStartOf="@+id/btnCC"
        app:layout_constraintTop_toTopOf="@+id/btnCC"
        app:shapeAppearanceOverlay="@style/roundedImageViewRounded"
        app:srcCompat="@drawable/claustro" />

    <FrameLayout
        android:id="@+id/frame_layout"
        android:layout_width="411dp"
        android:layout_height="449dp"
        app:layout_constraintBottom_toTopOf="@+id/btnCAQP"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>